# AGENTS.md — 프로젝트 개발 가이드

이 문서는 AI 에이전트와 기여자가 PC Spec Checker 프로젝트를 개발할 때 지켜야 할 규칙을 정리한 것입니다.

## 프로젝트 개요

Linux/macOS/Windows 하드웨어 사양 수집 CLI 도구. Go 표준 라이브러리 외 승인된 의존성은 아래와 같습니다.

**승인된 외부 의존성** (추가 시 사전 논의 필요):
- `github.com/wkqco33/wcli` — CLI 프레임워크
- `github.com/wkqco33/LLM_client_go` — LLM 클라이언트 (`analyzer/` 패키지, `--ai` 기능)

```
main.go              # 진입점 (flag 파싱, 흐름 조정)
model/               # 데이터 모델 (SystemInfo 등)
collector/           # OS별 정보 수집 (factory + linux/darwin/windows)
formatter/           # 수집 데이터를 콘솔 텍스트로 포맷팅
analyzer/            # AI 사양 분석 (LLM 클라이언트, --ai 플래그)
```

## 개발 워크플로우 (TDD)

이 프로젝트는 **TDD(테스트 주도 개발)** 방식을 따릅니다. 모든 변경은 다음 순서로 진행합니다.

1. **실패하는 테스트 먼저 작성**: 구현하려는 동작을 검증하는 테스트를 먼저 추가합니다.
2. **최소 구현으로 테스트 통과**: 테스트를 통과할 만큼만 구현합니다.
3. **리팩터링**: 코드를 정리하고 중복을 제거합니다. 이 단계에서도 테스트가 통과해야 합니다.

### 테스트 실행 규칙

- 구현을 변경할 때마다 반드시 `go test ./...`를 실행해 전체가 통과하는지 확인합니다.
- 커밋 전에는 `go vet ./...`와 `gofmt -l .`도 실행해 문제가 없는지 확인합니다.
- 테스트는 빌드 태그(`//go:build`)를 활용해 OS 독립적인 로직은 어느 플랫폼에서든 실행 가능하게 유지합니다.

```bash
task test            # 모든 유닛 테스트
go test ./...        # go test 직접 실행
task verify          # fmt + test + build 전체 검증
```

### OS별 구현체 테스트 원칙

- `collector/linux`, `collector/darwin`, `collector/windows`는 OS 의존적입니다.
- 실제 시스템 호출(`/proc`, `sysctl`, `wmic`, `lspci`)에 의존하는 테스트는 같은 빌드 태그를 사용합니다.
- **순수 파싱/검증 로직은 빌드 태그 없는 파일로 분리**하고, 어느 플랫폼에서든 테스트 가능하게 합니다.
  - 예: `vmstat_parse.go`, `vendor_detect.go` (태그 없음 + 전용 테스트 파일)

## 코드 스타일

- `gofmt` 형식을 준수합니다. 수정 후 `gofmt -l .` 결과가 비어 있어야 합니다.
- Go 표준 라이브러리만 사용하고, 새 외부 의존성을 추가하지 않습니다.
- 주석은 한 줄 요약 형태로 간결하게 작성합니다. 장황한 설명, AI 독백, 실행 흐름을 나열하는 주석은 금지합니다.

## 브랜치/커밋

- 작은 단위로 커밋하고, 커밋 메시지는 한국어로 간결하게 작성합니다.
- 테스트, 린트, 포맷이 모두 통과한 상태에서만 커밋합니다.

## 성능

- 수집 작업이 여러 독립 항목일 때는 병렬 처리(`sync.WaitGroup`)를 사용합니다 (Linux `CollectAll` 참고).
- 외부 명령어(스팬)를 반복 호출하지 않도록, 여러 값을 한 번의 호출로 수집합니다.

## OSS 규칙

- `LICENSE`(MIT), `CONTRIBUTING.md`, `SECURITY.md`의 기여·보안 정책을 준수합니다.
- 시크릿, 개인정보, 빌드 산출물(바이너리)을 커밋하지 않습니다.
