# 기여 가이드 (Contributing)

PC Spec Checker 프로젝트에 기여해 주셔서 감사합니다. 기여 전 아래 가이드를 확인해 주세요.

## 개발 환경

- Go 1.25.5 이상
- 외부 의존성 없이 Go 표준 라이브러리만 사용합니다.

## 개발 워크플로우 (TDD)

이 프로젝트는 테스트 주도 개발(TDD)을 따릅니다.

1. 실패하는 테스트를 먼저 작성합니다.
2. 최소 구현으로 테스트를 통과시킵니다.
3. 리팩터링하고, 이 단계에서도 테스트가 통과해야 합니다.

자세한 규칙은 [AGENTS.md](AGENTS.md)를 참고하세요.

## 커밋 전 확인

모든 변경 후 아래 명령어가 문제없이 통과해야 합니다.

```bash
gofmt -l .      # 포맷 확인 (비어 있어야 함)
go vet ./...    # 정적 분석
go test ./...   # 전체 테스트
```

## 브랜치/커밋 규칙

- 작은 단위로 커밋하고, 커밋 메시지는 한국어로 간결하게 작성합니다.
- 기능 추가는 `feature/`, 버그 수정은 `fix/` 브랜치에서 작업합니다.
- 완성 후 PR을 통해 `master` 브랜치로 병합합니다.

## PR 절차

1. 저장소를 fork하고 기능/버그 수정 브랜치를 생성합니다.
2. 변경 사항을 테스트, 린트, 포맷 검증과 함께 커밋합니다.
3. `master` 브랜치를 대상으로 PR을 생성합니다.
4. PR 설명에 변경 내용과 테스트 결과를 요약합니다.

## 이슈 보고

- 버그, 기능 제안, 개선 사항은 GitHub Issues에 등록해 주세요.
- 버그는 재현 방법, 예상 동작, 실제 동작을 포함해 주세요.
